# Project memory (newest first)

## 2026-03-31

- **Overview “New feature” + workspace picker:** Dashboard header ([`page.tsx`](src/app/(main)/page.tsx)) replaces sole **Go to Workspaces** with **New feature** (opens [`NewFeatureModal`](src/components/NewFeatureModal.tsx)) and a secondary **Workspaces** text link. Modal adds a **Workspace** `<select>` under feature name (defaults to first workspace on Overview, current route workspace in [`WorkspaceDetailClient`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx)); create uses chosen `workspace_id`. Overview flow: `POST /api/features`, then [`sessionStorage` bootstrap](src/lib/newFeatureBootstrap.ts) + `router.push` to `/workspaces/[id]?feature=…`; hydrate in `WorkspaceDetailClient` consumes the key and runs the same post-create path as in-modal create (user message persist + `infer-questions`). Modal overlay uses `position: fixed`. [`handleStartFeature`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) respects `workspace_id` from the form and navigates to that workspace.

- **Settings, profile, LLM usage:** Sidebar footer is a **profile row** (avatar initials, display name) that opens a **Radix popover** upward with email header, **Settings** → [`/settings`](src/app/(main)/settings/page.tsx), and **Log out**. Settings page: **Profile** (display name + email via Supabase `user_metadata` / `updateUser`) and **Token usage** (range 7d / 30d / all) from [`GET /api/me/usage`](src/app/api/me/usage/route.ts). New table **`ai_usage_events`** ([`supabase/schema.sql`](supabase/schema.sql), [`supabase/migration-ai-usage.sql`](supabase/migration-ai-usage.sql), RLS in [`supabase/rls-policies.sql`](supabase/rls-policies.sql)); [`recordAiUsage`](src/lib/ai/recordUsage.ts) logs provider usage from **discuss**, **infer**, **prd**, **competitor**, **infer-questions**, and **knowledge** image OCR ([`extractKnowledgeText`](src/lib/knowledge/extractText.ts)). APIs: [`/api/me/profile`](src/app/api/me/profile/route.ts) GET/PATCH. Dependency: **`@radix-ui/react-popover`**.

## 2026-03-29

- **PRD revision from composer:** [`handleSend`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) routes to [`POST /api/agents/prd`](src/app/api/agents/prd/route.ts) when `featureStatus === "done"` or the last non-system agent bubble is PRD with a non-pending status (`done` / `needs_review`). Begins a PRD draft, streams via `consumePrdStream`, finalizes with `finalizePrdAndPersistAssistant`, PATCHes feature back to `done`, and mirrors competitor-stage error handling (recovery prompt + `review` on failure). Only blocks sends while a PRD bubble is `pending`. Composer placeholder when done: *Describe changes you want in the PRD…*

- **Removed issues feature (clean slate):** Deleted global `/issues` page and nav link, feature Issues panel, `GenerateIssuesModal`, all issue API routes under `src/app/api/`, and issue-specific libs/tests. Dropped `feature_issues` from [`supabase/schema.sql`](supabase/schema.sql) and [`supabase/rls-policies.sql`](supabase/rls-policies.sql); removed [`supabase/migration-feature-issues.sql`](supabase/migration-feature-issues.sql) and [`supabase/migration-inference-issues-metadata.sql`](supabase/migration-inference-issues-metadata.sql). Added [`supabase/migration-drop-feature-issues.sql`](supabase/migration-drop-feature-issues.sql) — **run once in the Supabase SQL editor** on any database that already had `feature_issues`. Updated [`database.types.ts`](src/lib/database.types.ts). [`discuss-context.ts`](src/lib/discuss-context.ts) + [`discuss/route.ts`](src/app/api/features/[id]/discuss/route.ts) no longer load a saved backlog from the DB. [`WorkspaceDetailClient.tsx`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx): approving competitor analysis again streams the PRD (`finalizePrdAndPersistAssistant` returns the assistant line for the chat bubble). [`ChatInterface.tsx`](src/components/ChatInterface.tsx): removed `issues_prompt`. [`agent-prompts.ts`](src/lib/agent-prompts.ts): removed speqtr HTML comment markers for automated issue creation.

## 2026-03-27

- **Issues panel primary button:** [`FeatureIssuesPanel.module.css`](src/components/issues/FeatureIssuesPanel.module.css) `.btnPrimary` used only color overrides without base `.btn` rules (padding, radius, font, cursor), so **Regenerate…** looked like a native button. Fixed with `composes: btn` plus `.btnPrimary:hover` (aligned with [`knowledge/page.module.css`](src/app/(main)/knowledge/page.module.css)). **Refresh** uses `.refreshBtn` (`composes: btn` + hover) so primary hover is not overridden by a shared `.btn:hover`.

- **`infer-questions` uses `featureId` + `gpt-4o-mini`:** [`POST /api/agents/infer-questions`](src/app/api/agents/infer-questions/route.ts) loads `name`, `purpose`, `requirements` from `features` when `featureId` is present and fills any empty body fields (RLS-scoped). Uses `gpt-4o-mini` for structured `Output.object` (avoids `AI_APICallError` seen with `gpt-5.4-2026-03-05` on some keys). [`inferQuestionsSchema.ts`](src/lib/inferQuestionsSchema.ts): no `.transform()` / `.default()` on provider-facing objects, `.strict()` on objects — OpenAI structured outputs require full `required` lists; previous schema caused `Invalid schema for response_format 'clarifying_questions'`. Dev-only `detail` on 500 errors. Debug ingest `fetch` instrumentation removed from this route.

- **Feature pipeline: questions before inference artifact:** New features no longer fall through to streaming inference when `infer-questions` fails or returns too few items. [`handleStartFeature`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) creates the feature first, then loads questions; on failure shows an in-chat banner with **Retry** ([`retryPreInferenceQuestions`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx), [`page.module.css`](src/app/(main)/workspaces/[id]/page.module.css)). Feature create errors stay in [`NewFeatureModal`](src/components/NewFeatureModal.tsx) via `submitError`. For persisted features, [`consumeInferenceStream`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) uses `bufferDocumentUntilDone` + `dropParsedQuestions` so the document panel stays empty until completion and stream JSON clarifiers are ignored; no `clarifying_questions` metadata on assistant inference messages; [`handleSend`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) matches. Removed hydration that reopened the clarifying modal from saved inference metadata. [`ChatInterface`](src/components/ChatInterface.tsx): issues CTA **Looks good — generate issues**. Modal: primary **Start**, upload zone deferred with copy ([`NewFeatureModal.module.css`](src/components/NewFeatureModal.module.css)).

- **Light theme enforcement + clarifying modal:** [`layout.tsx`](src/app/layout.tsx) exports `viewport.colorScheme: 'light'` (meta `color-scheme`) and applies `geist.className` with the variable so the font loads reliably. [`globals.css`](src/app/globals.css): `html { color-scheme: light; }` and a second `:root` block after `@layer base` re-asserts legacy shell tokens so layered CSS cannot override paper colors. [`ClarifyingQuestionsModal.module.css`](src/components/ClarifyingQuestionsModal.module.css) modal background uses `var(--bg-surface)` instead of hardcoded `#1e1e1e`.

## 2026-03-26

- **Light “paper studio” theme:** Default UI is now warm off-white (`:root` in [`globals.css`](src/app/globals.css)) with legacy vars (`--bg-color`, `--accent-color` teal `#2f6f62`, etc.) aligned to shadcn tokens (near-black primary, muted teal ring). `.dark` retains full legacy + shadcn dark palette for a future toggle (not wired on `html`). Subtle grid on `body`; Geist-only typography ([`layout.tsx`](src/app/layout.tsx), removed Inter). New [`public/speqtr-logo-light.svg`](public/speqtr-logo-light.svg) (dark wordmark); [`Sidebar.tsx`](src/components/Sidebar.tsx) and [`animated-characters-login-page.tsx`](src/components/ui/animated-characters-login-page.tsx) use it. Login column: purple slab → muted teal `#3a6b62`, charcoal slab `#3a3a36`, single light grid overlay. Softer modal scrims/shadows: [`knowledge/page.module.css`](src/app/(main)/knowledge/page.module.css), [`NewFeatureModal.module.css`](src/components/NewFeatureModal.module.css), [`GenerateIssuesModal.module.css`](src/components/GenerateIssuesModal.module.css), [`ClarifyingQuestionsModal.module.css`](src/components/ClarifyingQuestionsModal.module.css). [`ChatInterface.module.css`](src/components/ChatInterface.module.css) agent avatar text uses `var(--primary-foreground)`.

## 2026-03-27

- **Pre-inference Q&A + backlog discussion chat:** New `POST` [`/api/agents/infer-questions`](src/app/api/agents/infer-questions/route.ts) returns structured questions ([`inferQuestionsSchema.ts`](src/lib/inferQuestionsSchema.ts), tests in [`inferQuestionsSchema.test.ts`](src/lib/inferQuestionsSchema.test.ts)). [`handleStartFeature`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) runs questions before streaming inference; [`runInitialInference`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) centralizes the infer stream + persist. [`buildInferenceSystem`](src/lib/context.ts) no longer appends `INFERENCE_CLARIFYING_JSON_RULES` (legacy no–`featureId` infer in [`infer/route.ts`](src/app/api/agents/infer/route.ts) still can). Clarifying modal: pre-draft copy, **Skip all**, Esc / × skip-all ([`ClarifyingQuestionsModal`](src/components/ClarifyingQuestionsModal.tsx)). [`handleClarifyComplete`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) saves v2 clarifications + user summary then re-runs inference (no auto-approve). After issues save, [`handleIssuesCommitted`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) sets `discussionMode`; composer hits `POST` [`/api/features/[id]/discuss`](src/app/api/features/[id]/discuss/route.ts) with `agent_type: 'discussion'` ([`ChatInterface`](src/components/ChatInterface.tsx) label **Assistant**). [`includeMessageForAgent`](src/lib/context.ts) excludes `discussion` from pipeline transcripts. [`issues/generate`](src/app/api/features/[id]/issues/generate/route.ts) adds `inference_clarifications` into `featureContext`.

- **Chat column width:** [`ChatInterface`](src/components/ChatInterface.tsx) message list wrapped in `.messageListColumn`; [`ChatInterface.module.css`](src/components/ChatInterface.module.css) uses shared `--chat-content-max: 800px` with `.inputWrapper` so transcript and composer share the same centered width.

- **Chat after issues + regenerate discoverability:** `handleSend` no longer blocks when the last bubble is `issues_prompt` with status `done` (only blocks while `needs_review`). Revision target comes from [`resolveRevisionAgent`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) (latest `inference` or `competitor` in the thread). Marking `needs_review` done now applies to competitor too. [`ChatInterface`](src/components/ChatInterface.tsx): optional `composerPlaceholder` when feature status is `done`. [`FeatureIssuesPanel`](src/components/issues/FeatureIssuesPanel.tsx): `onIssuesCommitted` posts a system line about **Regenerate**; parent wires `handleIssuesCommitted`.

- **422 / `[object Object]` on Generate issues:** Zod `flatten().toString()` produced `[object Object]`. [`parseIssueGenerationJson`](src/lib/issueGenerationSchema.ts) now builds a readable string from `result.error.issues`. [`GenerateIssuesModal`](src/components/GenerateIssuesModal.tsx) uses `formatGenerateError()` for any non-string `error` from the API.

- **Skip PRD in feature pipeline; gate → issue generation:** Approving competitor analysis no longer streams `/api/agents/prd` (previous block left as a comment in [`WorkspaceDetailClient.tsx`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx)). Chat shows an `issues_prompt` message with **Looks good — generate issues** ([`ChatInterface.tsx`](src/components/ChatInterface.tsx)); approve PATCHes feature `done`, opens `?panel=issues`, and bumps `launchIssuesGenerateToken`. [`FeatureIssuesPanel`](src/components/issues/FeatureIssuesPanel.tsx) opens [`GenerateIssuesModal`](src/components/GenerateIssuesModal.tsx) with `autoStartGenerate` and `onLaunchGenerateConsumed` resets the token. Composer PRD revision disabled (`isPrd` false; endpoint never `/api/agents/prd`). Hydration: `needs_review` for `review` + `issues_prompt`; synthetic copy updated. **Issue prompt:** [`issueGenerationPrompt.ts`](src/lib/issueGenerationPrompt.ts) aligned to detailed ROLE/TASK/PHASE/RULES/CONTEXT (document-grounded Phase 1).

- **Feature chat: Artifacts right pane + Issues toggle:** Replaced the old feature artifacts modal with URL-driven `?panel=artifacts` and optional `artifact=<uuid>` ([`FeatureArtifactsPanel`](src/components/artifacts/FeatureArtifactsPanel.tsx)). Chat header always shows **Artifacts** and **Issues** during feature chat; **X** calls `closeRightPane` (clears `panel` / `artifact`). [`WorkspaceDetailClient.tsx`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx): `openArtifactsPanel` / `openIssuesPanel` with PRD-dirty confirm; document actions clear panel via `clearPanelSearchParams`. Artifact detail: single markdown **Download** (`downloadMarkdownFile`); document toolbar uses one **Download** instead of split export. Removed Collapse Document, Back to document, and `FeatureArtifactsModal` component files.

- **Issue generation prompt + PRD-depth epic schema:** Replaced the inline PM prompt in [`/api/features/[id]/issues/generate`](src/app/api/features/[id]/issues/generate/route.ts) with the senior-GPM template in [`issueGenerationPrompt.ts`](src/lib/issueGenerationPrompt.ts). Live Phase 1 Q&A is not available server-side; the prompt states that inference + competitor artifacts **substitute** for the discovery interview. Zod in [`issueGenerationSchema.ts`](src/lib/issueGenerationSchema.ts) now validates the full epic (goals, personas, metrics, risks, DoD, etc.) and stories (`id`, narrative, min 3 ACs, dependencies, notes). [`formatEpicMarkdownForStorage`](src/lib/issueGenerationSchema.ts) flattens epic fields into one markdown `description` for `feature_issues`. [`GenerateIssuesModal`](src/components/GenerateIssuesModal.tsx) passes `persona` / `narrative` / merged `notes` (scope + model notes) into commit. Tests: [`issueGenerationSchema.test.ts`](src/lib/issueGenerationSchema.test.ts).

## 2026-03-25

- **Inference-driven issues + panel UX (pivot):** Issues are generated with `POST` [`/api/features/[id]/issues/generate`](src/app/api/features/[id]/issues/generate/route.ts) from latest **inference** + **competitor** artifacts (Zod validation in [`issueGenerationSchema.ts`](src/lib/issueGenerationSchema.ts)); [`commit`](src/app/api/features/[id]/issues/commit/route.ts) accepts `generated_from`, per-epic/story `due_date`, extended `status`/`priority`. Removed PRD `POST /issues/preview` and the old PRD backlog modal component. Schema: [`schema.sql`](supabase/schema.sql) + incremental [`migration-inference-issues-metadata.sql`](supabase/migration-inference-issues-metadata.sql) (`due_date`, `generated_from`, richer status/priority checks). UI: [`FeatureIssuesPanel`](src/components/issues/FeatureIssuesPanel.tsx) + [`IssueDetailLayout`](src/components/issues/IssueDetailLayout.tsx) (80/20, epic lists child stories); feature URL `?panel=issues` + chat **Issues** ([`WorkspaceDetailClient.tsx`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx)); [`GenerateIssuesModal`](src/components/GenerateIssuesModal.tsx). Workspace **Artifacts** is documents-only (issues tab removed). Global [`/issues`](src/app/(main)/issues/page.tsx) uses the same detail layout. Helper: [`getLatestCompletedArtifactByKind`](src/lib/artifact-persistence.ts). The deterministic [`prdBacklogParse.ts`](src/lib/prdBacklogParse.ts) library remains for tests/other use but is no longer the primary create path.

- **Rich export + titles + feature artifacts:** `POST` [`/api/features/[id]/artifacts/export-docx`](src/app/api/features/[id]/artifacts/export-docx/route.ts) converts markdown to `.docx` via [`markdownToDocx.ts`](src/lib/markdownToDocx.ts) (`docx` package). [`artifactExport.ts`](src/lib/artifactExport.ts) `downloadDocxForFeature` + menu **Download Word (.docx)** on [`DocumentExportSplitButton`](src/components/DocumentExportSplitButton.tsx) / list export. [`deriveDocumentTitle.ts`](src/lib/deriveDocumentTitle.ts) drives panel heading (kind badge + derived title + Markdown). `GET` [`/api/features/[id]/artifacts?summary=1`](src/app/api/features/[id]/artifacts/route.ts) uses [`listFeatureArtifactsSummary`](src/lib/artifact-persistence.ts) (no `body`). Feature chat **Artifacts** opens the right pane ([`FeatureArtifactsPanel`](src/components/artifacts/FeatureArtifactsPanel.tsx), `?panel=artifacts`); unsaved PRD confirm via `prdContentDirtyRef` when switching panels.

- **Artifact export (Markdown + copy):** `GET` [`/api/features/[id]/artifacts/[artifactId]`](src/app/api/features/[id]/artifacts/[artifactId]/route.ts) returns `body` and metadata for RLS-scoped reads. [`artifactExport.ts`](src/lib/artifactExport.ts) builds safe `.md` filenames, triggers download, clipboard copy. [`DocumentExportSplitButton.tsx`](src/components/DocumentExportSplitButton.tsx): split **Download** + menu (**Download Markdown**, **Copy as-is**); [`ArtifactListExportSplitButton`](src/components/DocumentExportSplitButton.tsx) fetches then exports. **Document panel** ([`WorkspaceDetailClient`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) + [`page.module.css`](src/app/(main)/workspaces/[id]/page.module.css)): title `{name} — … · Markdown`, **Save** (PRD), split download, **X** closes split view. **Tables:** Actions column on [`/artifacts`](src/app/(main)/artifacts/page.tsx) and workspace artifacts list.

## 2026-03-23

- **Inference rework loop:** **Update feature inference** sits in the chat action row next to **View feature inference** (inference `needs_review`) in [`ChatInterface.tsx`](src/components/ChatInterface.tsx); closes clarifying flow without saving or auto-approving, same `clarifyShownForRef` behavior as modal X close; `inferenceReviseHint` + `focusComposerToken` for composer placeholder/focus. [`WorkspaceDetailClient`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) `handleUpdateInferenceInstead` + `handleSend` marks prior `needs_review` inference bubbles `done` before a new revision. Prompt: [`INFERENCE_REVISION_FROM_TRANSCRIPT`](src/lib/agent-prompts.ts) in [`buildInferenceSystem`](src/lib/context.ts).

- **List + global search (metadata only):** Optional `q` on `GET` [`/api/workspaces`](src/app/api/workspaces/route.ts), [`/api/features`](src/app/api/features/route.ts), [`/api/knowledge`](src/app/api/knowledge/route.ts), [`/api/artifacts`](src/app/api/artifacts/route.ts), [`/api/workspaces/[id]/artifacts`](src/app/api/workspaces/[id]/artifacts/route.ts) filters with `ilike` on names/titles/kinds/status (not messages, artifact body, or chunk text). Shared helpers: [`escapeIlike.ts`](src/lib/search/escapeIlike.ts), [`artifactsFlatList.ts`](src/lib/search/artifactsFlatList.ts). Aggregated [`GET /api/search`](src/app/api/search/route.ts) (limit 10 per section, includes `href`). UI: debounced search on dashboard, workspaces, knowledge (with `?highlight=` scroll + row highlight), artifacts, workspace feature/artifact lists; [`GlobalSearch`](src/components/GlobalSearch.tsx) in sidebar with ⌘/Ctrl+K. Hook: [`useDebouncedValue`](src/lib/hooks/useDebouncedValue.ts).

- **Login marketing column:** Desktop animated column uses `bg-background` + `border-r` (no purple gradient); logo keeps original SVG colors (`brightness-0 invert` removed). Footer links use `text-muted-foreground`; grid + blurs tuned for light panel (dark grid lines; `primary` glows). Mobile header logo stays larger (`max-h-14`, `max-w-sm`).

- **Sidebar Speqtr logo size:** Logo uses full sidebar row width with `max-height: 52px` and `object-fit: contain` so the wordmark isn’t stuck at ~50×28px.

- **Rebrand to Speqtr:** Product title/metadata → **Speqtr** ([`layout.tsx`](src/app/layout.tsx)); favicon via [`public/speqtr-logo.svg`](public/speqtr-logo.svg). Sidebar ([`Sidebar.tsx`](src/components/Sidebar.tsx) + CSS) and login marketing column / mobile header ([`animated-characters-login-page.tsx`](src/components/ui/animated-characters-login-page.tsx)) use the logo. `package.json` / lock `name`: `speqtr`. [`schema.sql`](supabase/schema.sql) header comment updated. Root `speqtr logo.svg` removed after copy to `public/`.

## 2026-03-23

- **Vercel `/api/knowledge` + PDF:** Production failed loading `pdf-parse` with `ReferenceError: DOMMatrix is not defined` (pdfjs expects browser globals on Node). Added [`pdfDomPolyfill.ts`](src/lib/knowledge/pdfDomPolyfill.ts) (imported first from [`extractText.ts`](src/lib/knowledge/extractText.ts)) assigning `DOMMatrix`, `DOMPoint`, `DOMRect`, `Path2D`, `ImageData` from `@napi-rs/canvas` onto `globalThis` when missing.

- **Debug session (7267bb) `/api/knowledge` GET:** Local repro logs showed `getClaims` OK (`hasSub: true`), `knowledge_documents` query OK with `rowCount: 2` — no code defect in auth or list path. Instrumentation in `require-user.ts` / `api/knowledge/route.ts` removed after analysis. Vercel-only issues would need prod DB migration, env parity, or fresh session cookie (not captured by local ingest).

- **Knowledge base transparency in chat:** `assembleFeatureContext` now returns `knowledgeBase: { consulted, sources }` (unique document labels from retrieved chunks). Agent routes (`infer`, `competitor`, `prd`) send it in response header `x-pm-knowledge-base` (URL-encoded JSON). [`WorkspaceDetailClient`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx) reads the header and inserts a **System** chat line before streaming: lists source names when excerpts are used, or explains when the library was searched but nothing passed the relevance threshold. Helpers: [`httpHeaders.ts`](src/lib/knowledge/httpHeaders.ts), [`chatNotice.ts`](src/lib/knowledge/chatNotice.ts).

- **PDF knowledge ingestion (pdfjs worker + upload UI truth):** Next was bundling `pdf-parse`/`pdfjs-dist`, so PDF.js fell back to a “fake worker” and resolved a broken path under `.next/dev/server/`. Fix: [`serverExternalPackages`](next.config.ts) for `pdf-parse`, `pdfjs-dist`, `@napi-rs/canvas`, plus [`ensurePdfJsWorker()`](src/lib/knowledge/extractText.ts) calling `PDFParse.setWorker` with a `file:` URL from `require.resolve('pdfjs-dist/legacy/build/pdf.worker.mjs')`. **Upload queue:** API can return **201** with `status: 'failed'` after upload; [`KnowledgeBulkUpload`](src/components/KnowledgeBulkUpload.tsx) now treats that as **Failed** and shows `error_message` instead of **Done**.

- **Knowledge bulk upload UI:** Added [`KnowledgeBulkUpload`](src/components/KnowledgeBulkUpload.tsx) with multi-file picker, multi-file drag-and-drop, sequential queue, per-row status (queued / uploading / done / failed), XMLHttpRequest byte progress for the active file, client-side validation aligned with `/api/knowledge`, “Clear finished”, and unmount abort. Wired on [`/knowledge`](src/app/(main)/knowledge/page.tsx); removed unused dropzone rules from [`page.module.css`](src/app/(main)/knowledge/page.module.css). **Fix:** snapshot `File[]` with `Array.from(input.files)` before resetting `input.value` (live `FileList` was empty after reset, so nothing queued); file input “visually hidden” CSS avoids `pointer-events: none` blocking programmatic open in some browsers.

- **Global knowledge base (files + pasted text, chunked RAG):** Per-user `knowledge_documents` / `knowledge_chunks` with pgvector (`text-embedding-3-small`, 1536), `match_knowledge_chunks` RPC, private Storage bucket `knowledge` (`{user_id}/...`). API: `GET`/`POST` `/api/knowledge` (multipart upload or JSON `{ sourceKind: "text", title?, content }`), `DELETE` `/api/knowledge/[id]`. Ingestion extracts PDF (`pdf-parse` / `PDFParse`), DOCX (`mammoth`), plain text, images via `gpt-4o-mini` vision; chunks + async-safe cleanup on failure. `assembleFeatureContext` injects **Knowledge base (retrieved excerpts)** from feature name/purpose/requirements + optional revision when `userId` is passed; wired in infer/competitor/prd agent routes. UI: `/knowledge` (upload dropzone, Add text modal, list, delete, polling while `processing`), Sidebar **Knowledge base**. SQL: [`supabase/migration-knowledge-base.sql`](supabase/migration-knowledge-base.sql), updates to [`schema.sql`](supabase/schema.sql) + [`rls-policies.sql`](supabase/rls-policies.sql). Lib: [`src/lib/knowledge/*`](src/lib/knowledge/), [`src/lib/database.types.ts`](src/lib/database.types.ts). **Existing Supabase projects:** run the migration SQL in the dashboard (includes bucket + storage policies).

- **Faded grid background + chat alignment:** Added app-wide **subtle grid** on `body` via stacked `repeating-linear-gradient` layers using `--grid-size` and `--grid-line` in [`globals.css`](src/app/globals.css). **Chat:** user rows use `flex-direction: row-reverse`, `margin-left: auto`, right-aligned header/body/actions; agent/system/loading rows stay **left** with `margin-right: auto`; user avatars get a distinct surface/border treatment. Files: [`src/app/globals.css`](src/app/globals.css), [`src/components/ChatInterface.module.css`](src/components/ChatInterface.module.css).

## 2026-03-22

- **PRD editor UX (lists, links, tables, dividers):** Tailwind preflight had removed list markers; globals `a` removed link styling—restored **visible bullets/numbers** and **accent-colored underlined links** under `.docProseMirror` and `.docPaperPreview`. Added **Divider** toolbar control (`setHorizontalRule`), **group separators** and subtle toolbar chrome, stronger **table** header/zebra/hover and **hr** styling. **Table bubble menu** (`BubbleMenu` from `@tiptap/react/menus`, `pluginKey` `prdTableStructureMenu`) when the cursor is in a table: add/delete rows & columns, toggle header row, merge/split, delete table. Dependency: `@tiptap/extension-bubble-menu`. Files: [`page.module.css`](src/app/(main)/workspaces/[id]/page.module.css), [`PrdDocumentEditor.tsx`](src/components/PrdDocumentEditor.tsx), `package.json`. (Text color / highlight left optional per plan.)

- **Document pane full-width layout:** PRD/inference/competitor panel CSS updated so the editor fills the split pane: `min-width: 0` / `flex: 1` on `.container`/`.layout`, chat `35%` cap + editor `flex: 1 1 0` (remaining width), `.editorContent` and `.docPaper` use `width/max-width: 100%` (removed 720px column), `.docEditorWrap` stretches TipTap; ProseMirror gets `word-break`/`overflow-wrap` and a saner `min-height`. File: `src/app/(main)/workspaces/[id]/page.module.css`.

- **WYSIWYG PRD document panel (no paid services):** Replaced the raw markdown `<textarea>` with [`PrdDocumentEditor`](src/components/PrdDocumentEditor.tsx): while the PRD agent streams (`documentPanelKind === "prd"` and `isLoading`), the panel shows a read-only **paper-style** preview via `react-markdown` + `remark-gfm`; when idle, the same markdown is edited in **TipTap** (StarterKit, Link, Table, Placeholder, Gapcursor) with a small formatting toolbar. Persistence stays **markdown text** through [`prdMarkdownBridge`](src/lib/prdMarkdownBridge.ts) (`marked` → HTML for the editor, `turndown` + `turndown-plugin-gfm` → markdown on change). **Inference** and **competitor** panels use the same preview (read-only). Added document/preview/toolbar styles in [`page.module.css`](src/app/(main)/workspaces/[id]/page.module.css), ProseMirror base CSS import, ambient types for `turndown-plugin-gfm`, and `**/*.d.ts` in [`tsconfig.json`](tsconfig.json). Wired from [`WorkspaceDetailClient.tsx`](src/app/(main)/workspaces/[id]/WorkspaceDetailClient.tsx).

- **Inference/competitor in document panel (like PRD):** Chat shows short stubs (“Feature inference is ready…”, “Competitor analysis is ready…”) while full text streams and lives in `inferenceDocument` / `competitorDocument` in the right-hand panel (`documentPanelKind`). Hydration moves legacy long `feature_messages` bodies into those buffers and replaces chat with the same stubs. `ChatInterface` adds “View feature inference” / “View competitor analysis” (with read-only panel); PRD stays editable with Save. Files: `WorkspaceDetailClient.tsx`, `ChatInterface.tsx`.

- **Inference/competitor artifacts + Artifacts navigation:** Assistant messages with `agent_type` `inference` or `competitor` now also append versioned `feature_artifacts` rows (`appendCompletedAgentArtifact`, linked via `source_message_id`). Sidebar **Recent Features** removed; **Artifacts** link goes to `/artifacts` listing all non-draft artifacts (join feature + workspace). Workspace list view adds **Features | Artifacts** tabs (`?view=artifacts`) backed by `GET /api/workspaces/[id]/artifacts`. New: `GET /api/artifacts`, `src/app/(main)/artifacts/page.tsx` + `page.module.css`. Updated: `src/lib/artifact-persistence.ts`, `src/app/api/features/[id]/messages/route.ts`, `src/components/Sidebar.tsx`, `WorkspaceDetailClient.tsx`, `workspaces/[id]/page.module.css`.

- **Inference UX (real output vs placeholder + questions):** Fixed a race where `router.replace(?feature=)` ran before `streamingRef` was set, so feature hydration fetched empty `feature_messages` and replaced the live chat with the synthetic “Loaded from workspace. Approve to continue…” row. `streamingRef` is now set **before** `router.replace` when a feature id exists; `finally` clears it after the infer request. **Prompts:** inference must deliver substantive analysis with explicit assumptions when inputs are thin; machine-readable clarifying questions are **0–5** and only after the full narrative, not a substitute for inference (`src/lib/agent-prompts.ts`). Files: `WorkspaceDetailClient.tsx`, `agent-prompts.ts`.

- **Global `*` reset vs Tailwind spacing:** Removed `margin: 0; padding: 0` from the universal selector in [`src/app/globals.css`](src/app/globals.css). Those rules were **unlayered**, so they overrode Tailwind `@layer utilities` and made shadcn `Input`/`Button` padding and utilities like `space-y-*` appear missing. Kept `box-sizing: border-box` only (with `::before`/`::after`).

- **Animated characters login (Tailwind v4 + shadcn):** Initialized shadcn (base-nova) with `@tailwindcss/postcss`, `components.json`, `src/lib/utils.ts`, and UI primitives (`button`, `input`, `label`, `checkbox`). `/login` uses `AnimatedCharactersLoginPage` (`src/components/ui/animated-characters-login-page.tsx`): split layout with illustrated characters, Supabase email/password sign-in and sign-up, check-email + resend flow, post-confirm hint, and **Google OAuth** via `signInWithOAuth` → `/auth/callback`. Characters **close all eyes** while the password field is focused; purple peek animation runs only when the password is visible and the field is not focused. Removed `login.module.css`. Global `button`/`input` resets that stripped shadcn styles were dropped; shadcn theme tokens in `:root` were tuned for a dark app shell and purple `primary` for the marketing column. Files: `postcss.config.mjs`, `tailwind.config.js`, `src/app/globals.css`, `src/components/ui/*`, `src/app/login/LoginForm.tsx`, `src/app/layout.tsx` (Geist variable from shadcn).

- **Post–sign-up email confirmation UX:** When `signUp` returns no `session` (Supabase **Confirm email** enabled), the login card shows a **Check your email** panel (`role="status"` / `aria-live="polite"`) with the address, spam hint, **Continue to sign in** (switches to sign-in mode, clears password, shows banner “After you confirm your email, sign in below”), and **Resend confirmation email** via `auth.resend({ type: 'signup', email })` with a 60s cooldown. If `session` is present (confirm disabled), behavior unchanged: refresh + redirect to `next`. Files: `src/app/login/LoginForm.tsx` (Tailwind login UI).

- **Supabase IAM (auth + RLS):** Added `@supabase/ssr` with cookie-based `createServerSupabaseClient` (`src/lib/supabase/server.ts`) and `createBrowserSupabaseClient` (`src/lib/supabase/browser.ts`). `requireUser()` + `getClaims()` gate API routes; middleware refreshes sessions and redirects unauthenticated users to `/login` (public: `/login`, `/auth/*`, `/api/*`). Workspaces gain `created_by → auth.users`; RLS policies are `to authenticated` with owner checks via joins (`supabase/migration-auth-iam.sql` for existing DBs, updated `supabase/rls-policies.sql` + `schema.sql`). UI: `/login` (email/password sign-in + sign-up), `/auth/callback`, POST `/auth/sign-out`, route group `(main)` for shell + sidebar. Lib DB helpers take `AppSupabase` as first argument (`artifact-persistence`, `prd-persistence`, `embeddings`, `context`). **Supabase dashboard:** enable Email provider; set Site URL and Redirect URLs to include `/auth/callback` (e.g. `http://localhost:3000/auth/callback` and production origin). **Next.js 16** warns that `middleware` is deprecated in favor of `proxy`—build still succeeds.

- **Vercel production deploy:** Linked project **`rushi-pm-tool`** under team **`rushi-projects`** (GitHub `rushilakare/storyboard` connected). Set env vars in Vercel: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (Production + Development), `OPENAI_API_KEY` (Production sensitive; Development non-sensitive—Vercel blocks sensitive → Development). Production URL: **https://rushi-pm-tool.vercel.app**. **Supabase client:** replaced eager `getSupabaseClient()` at module load with a **lazy `Proxy`** so `next build` on Vercel succeeds before runtime env is applied; real client still created on first use. Preview (PR) deployments: add the same three variables for **Preview** in the Vercel dashboard if CLI branch targeting fails. Files: `src/lib/supabase.ts`.

- **Git remote + GitHub auth:** `origin` is `https://github.com/rushilakare/storyboard.git`. GitHub CLI active account switched to **`rushilakare`** (`gh auth switch --user rushilakare`); `gh auth setup-git` wires `credential.https://github.com.helper` to `gh auth git-credential` so HTTPS Git uses that account. `main` pushed and tracks `origin/main`. (SSH had no local key in `~/.ssh`; restore `git@github.com:rushilakare/storyboard.git` after adding a key to GitHub if you prefer SSH.)

- **Git repository initialized:** Ran `git init` on `main`, staged the project (existing `.gitignore` keeps `.env*` and `.vercel` out of commits), and created root commit `c2be2e8` (“Initial commit: Next.js PM tool with workspaces, features, and agents”). No remote yet—add with `git remote add origin <url>` then `git push -u origin main`.

- **LLM-generated post-inference clarifying questions:** The inference agent now appends a delimiter (`<<<CLARIFYING_QUESTIONS_JSON>>>`) and a single-line minified `{"questions":[...]}` payload (historically 3–6 items; now 0–5 when prompts allow skipping redundant questions). Types include `single`, `multiple`, `multiple_with_other`, and `text` (freeform textarea). The chat stream shows narrative-only text; parsed questions drive `ClarifyingQuestionsModal` via `pendingClarifyingQuestions`. Assistant rows persist `metadata.clarifying_questions`; `inference_clarifications` on the feature is stored as v2 `{ v: 2, questions, answers }`. `assembleFeatureContext` formats v2 clarifications by question title; legacy flat jsonb rows still work. Closing the clarifier without finishing removes the dedupe ref so a refresh can reopen; switching features clears the ref.
  - Prompts: `src/lib/agent-prompts.ts` (`INFERENCE_CLARIFYING_JSON_RULES`), `src/lib/context.ts` (`buildInferenceSystem`), `src/app/api/agents/infer/route.ts` (legacy prompt).
  - Parsing/types: `src/lib/postInferenceQuestions.ts` (`splitInferenceDisplayBuffer`, `parseInferenceStreamComplete`, `formatClarificationSummary(questions, answers)`, `formatAnswerForQuestion`, `isInferenceClarificationsV2`).
  - Client: `src/app/workspaces/[id]/WorkspaceDetailClient.tsx` (`consumeInferenceStream`, hydrate reopen, v2 PATCH).
  - UI: `src/components/ClarifyingQuestionsModal.tsx`, `ClarifyingQuestionsModal.module.css` (`.textArea`), `src/components/ChatInterface.tsx` (`clarifyingQuestions` prop).

- **Feature artifacts + versioned PRDs (bodies out of chat):** Added `feature_artifacts` (per-feature `kind` + monotonic `version`, `is_draft` for streaming, optional `source_message_id`). PRD markdown lives in artifact rows; chat stores short assistant lines plus `metadata.artifact_ids` / `artifact_version`. Flow: `POST /prd` `{ beginDraft: true }` starts a draft; streaming autosaves `PUT` `{ content }`; completion uses `PUT` `{ content, finalize: true }` (and agent `onFinish` finalize, idempotent). Manual editor save uses `PUT` `{ content, replaceLatest: true }` to patch the latest completed revision without bumping version. `GET /prd`, feature hydrate, and `assembleFeatureContext` read latest **non-draft** PRD artifact, with fallback to legacy `prd_documents`. New: `src/lib/artifact-persistence.ts`, `src/app/api/features/[id]/artifacts/route.ts`, `src/app/api/features/[id]/artifacts/[artifactId]/route.ts` (PATCH `source_message_id`), `supabase/backfill_prd_to_feature_artifacts.sql`. Updated: `supabase/schema.sql`, `supabase/rls-policies.sql`, `src/lib/database.types.ts`, `src/lib/context.ts`, `src/lib/prd-persistence.ts` (status patch only), `src/lib/embeddings.ts` (merge metadata on embed), `src/app/api/features/[id]/prd/route.ts`, `src/app/api/features/[id]/route.ts`, `src/app/api/agents/prd/route.ts`, `src/app/workspaces/[id]/WorkspaceDetailClient.tsx`.

- **Agent context paths (inference / competitor / PRD) aligned:** Introduced `src/lib/agent-prompts.ts` with shared PRD output contract, transcript discipline, and strict competitor + inference output rules. `assembleFeatureContext` now (1) filters `feature_messages` by `agent_type` so each agent only sees relevant turns (e.g. competitor does not see prior PRD blobs; PRD sees inference + competitor only, not prior PRD chat rows), (2) injects saved `prd_documents.content` into the PRD system prompt for revisions while omitting that block during PRD continue mode to avoid duplicating the partial draft, (3) uses the same full PRD requirements as the legacy no–`featureId` prompt, (4) scopes semantic retrieval to the current feature, (5) raises PRD char budget slightly. Legacy agent routes import the same discipline strings for non–`featureId` calls.
  - New: `src/lib/agent-prompts.ts`.
  - Updated: `src/lib/context.ts`, `src/app/api/agents/prd/route.ts` (`omitSavedPrdDocument` when continuing), `src/app/api/agents/competitor/route.ts`, `src/app/api/agents/infer/route.ts`.

- **PRD streaming durability + recovery:** Mid-stream PRD text is no longer only in React state. Debounced `PUT /api/features/[id]/prd` during streaming, `beforeunload` + `navigator.sendBeacon` `POST` to the same resource (beacon-safe), server `streamText` `onFinish` upsert + `status: done` fallback, new feature status `generating` for interrupted runs, and a PRD editor recovery banner (continue from partial / regenerate / edit manually → `review`). PRD chat revisions now `PUT` the document after streaming. Agent `fetch` paths check `res.ok` before reading bodies. **Existing Supabase DBs:** run `supabase/migration-add-generating-status.sql` so `generating` is allowed.
  - Client: `src/app/workspaces/[id]/WorkspaceDetailClient.tsx`, `src/app/workspaces/[id]/page.module.css` (`.streamError`).
  - UI: `src/components/PrdRecoveryBanner.tsx`, `PrdRecoveryBanner.module.css`.
  - API: `src/app/api/features/[id]/prd/route.ts` (POST beacon upsert, shared upsert helper usage), `src/app/api/agents/prd/route.ts` (`continue` draft + `onFinish`).
  - Lib: `src/lib/prd-persistence.ts` (`upsertPrdDocument`, `patchFeatureStatus`).
  - Schema/types: `supabase/schema.sql`, `supabase/migration-add-generating-status.sql`, `src/lib/database.types.ts`.

- **Inline clarifying UI + auto-proceed:** Repositioned the post-inference questionnaire from a centered full-screen overlay to an inline card rendered above the chat input (Claude-style). Completing the questionnaire now auto-starts the competitor agent (no manual "Looks Good" click needed) and injects a summary of the user's answers as a visible chat message. The input area hides while the questionnaire is active.
  - UI: `src/components/ClarifyingQuestionsModal.tsx` (removed overlay wrapper), `ClarifyingQuestionsModal.module.css` (flow-level card), `src/components/ChatInterface.tsx` (new `clarifyingOpen`/`onClarifyComplete`/`onClarifyClose` props, inline render).
  - Logic: `src/app/workspaces/[id]/WorkspaceDetailClient.tsx` (`handleClarifyComplete` adds summary message + auto-calls `handleApprove("inference")`).
  - Helper: `src/lib/postInferenceQuestions.ts` (`formatClarificationSummary`).

- **Post-inference clarifying questionnaire:** Added a Claude-style multi-step modal that opens immediately after inference streaming completes. Supports single-choice, multi-choice, and multi-choice-with-other question types; each step is skippable via button or Esc. Answers are persisted as `inference_clarifications` (jsonb) on the feature row and injected into downstream agent prompts via `assembleFeatureContext`.
  - Schema: `supabase/schema.sql` (new `inference_clarifications jsonb` column), `src/lib/database.types.ts` (Row/Insert/Update).
  - API: `src/app/api/features/[id]/route.ts` (PATCH accepts `inference_clarifications`).
  - Config: `src/lib/postInferenceQuestions.ts` (5 typed questions).
  - UI: `src/components/ClarifyingQuestionsModal.tsx`, `ClarifyingQuestionsModal.module.css`.
  - Wire-up: `src/app/workspaces/[id]/WorkspaceDetailClient.tsx` (opens modal on inference done, PATCHes on complete/close, dedup guard via ref).
  - Context: `src/lib/context.ts` (appends structured clarifications to feature block).

- **Chat persistence + vector search (full plan execution):** Implemented durable chat message storage, context assembly, async embeddings, and semantic/lexical search.
  - **Schema:** `feature_messages` table (append-only, `sequence_num`, `token_count`, `search_vector` tsvector, `embedding` vector(1536)), HNSW index, FTS GIN index, `match_feature_messages` and `search_feature_messages` Postgres RPC functions, pgvector extension. Files: `supabase/schema.sql`, `supabase/rls-policies.sql`, `src/lib/database.types.ts`.
  - **Messages API:** `GET`/`POST` `/api/features/[id]/messages` with server-assigned `sequence_num`, `token_count`, and fire-and-forget embedding via `embedMessageAsync`. File: `src/app/api/features/[id]/messages/route.ts`.
  - **Embeddings:** `computeEmbedding` (text-embedding-3-small via AI SDK) and `embedMessageAsync` (update row with vector). File: `src/lib/embeddings.ts`.
  - **Context assembler:** `assembleFeatureContext(featureId, agentKind, options)` loads feature + recent messages (char-budget walk), optionally runs `retrieveSemanticContext` and injects as labeled SYSTEM block. Also `searchFeatureMessages` (FTS) and `hybridSearch` (RRF). File: `src/lib/context.ts`.
  - **Agent routes:** `infer`, `competitor`, `prd` accept `featureId`; if present, use `assembleFeatureContext` with `system` + `messages` format; legacy fallback preserved. Files: `src/app/api/agents/*/route.ts`.
  - **Client:** `WorkspaceDetailClient` persists user messages before agent calls, persists assistant messages after stream completes, hydrates from `/api/features/[id]/messages` on `?feature=` load (with status-aware `needs_review` on last agent message). File: `src/app/workspaces/[id]/WorkspaceDetailClient.tsx`.

- **Cursor skill (personal):** Added `chat-infra-architect` under `~/.cursor/skills/chat-infra-architect/` with `SKILL.md` (write/read/context/search mental model, Postgres schema, Redis hot path, context assembly, retrieval, scaling, failure modes) and `references/` (`token-counting.md`, `summarization-prompts.md`, `schema-migrations.md`, `vector-store-selection.md`). Description is third-person for discovery. No app code changed.

- **Workspace features & deep links:** `/workspaces/[id]` lists features for that workspace with “New feature”; `/workspaces/[id]?feature=[uuid]` opens chat + PRD with server-loaded state (draft / in progress / review+done). Sidebar and dashboard link to `?feature=`. `streamingRef` avoids overwriting UI while the inference stream runs after create. Files: `src/app/workspaces/[id]/page.tsx`, `WorkspaceDetailClient.tsx`, `page.module.css`, `src/components/Sidebar.tsx`, `src/app/page.tsx`, `page.module.css`.
- **Workspaces / Supabase:** Hardened Workspaces page when APIs return 500 (non-array JSON). Added `supabase/rls-policies.sql` (RLS + anon policies) and noted it in `schema.sql`. Files: `src/app/workspaces/page.tsx`, `supabase/rls-policies.sql`, `supabase/schema.sql`.
- **Env:** Added `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` to `.env.local` for local Next.js. File: `.env.local`.
